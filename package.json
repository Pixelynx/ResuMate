{
  "name": "resumate",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "start": "cd client && npm start",
    "build": "cd client && npm run build",
    "test": "cd client && npm test"
  },
  "dependencies": {},
  "devDependencies": {}
}